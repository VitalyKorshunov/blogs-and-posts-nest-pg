export class LoginSuccessDTO {
  accessToken: string;
  refreshToken: string;
}

export class AccessTokenViewDTO {
  accessToken: string;
}

export class UpdateTokensViewDTO {
  accessToken: string;
  refreshToken: string;
}
